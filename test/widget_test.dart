import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test', () {
    // This project currently does not include widget tests.
    // Keep a minimal test so `flutter test` can run in CI without failing.
    expect(true, isTrue);
  });
}
